import 'package:flutter/material.dart';

class AppCreateAccount extends StatelessWidget {
  const AppCreateAccount({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text("hjello"),),);
  }
}